<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panka.my_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MCA Previous Year"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Mca Previous Year">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PAGE_2"
            android:label="Exam Page" />
        <activity
            android:name=".YearListActivity"
            android:label="YearWise Question" />
        <activity
            android:name=".PdfViewerActivity"
            android:label="Pdf" />
        <activity android:name=".LoginPageActivity" />
        <activity android:name=".QuizActivity"
            android:label="Quiz"/>
        <activity android:name=".ScoreCardActivity"
            android:label="Your Score"></activity>
    </application>

</manifest>